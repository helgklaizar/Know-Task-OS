{
  "name": "@omni/dispatcher-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "source venv/bin/activate && uvicorn main:app --reload",
    "test": "source venv/bin/activate && pytest tests/"
  }
}
