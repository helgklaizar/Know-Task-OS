{
  "name": "@omni/knowledge-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "poetry run uvicorn backend.main:app --host 0.0.0.0 --port 8085 --reload",
    "build": "echo 'No build step for Python API'",
    "lint": "poetry run ruff check .",
    "format": "poetry run black ."
  }
}
